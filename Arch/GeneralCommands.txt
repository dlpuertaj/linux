- Install VirtualBox Guess Additions
    - $sudo pacman -Syu # Update system
    - $sudo reboot
    - $uname -r                        # View kernel version
    - $mhwd-kernel -li                 # View kernel version
    - $sudo pacman -S linux510-headers # Install Linux Headers for the kernel version
    - $sudo reboot
    - $sudo pacman -S gcc              # Install gcc
    - $sudo pacman -S make             # Install make
    - $sudo reboot
    - $df -hT                          # ?
    - $sudo reboot
    - $cd /run/media/dlpuerta/         # Go to mount (VBox GA)
    - $cd VBox_GAs_6.1.24/             # Go to VBox mount
    - $sudo ./VBoxLinuxAdditions.run   # Run install file
    - $sudo reboot
- Instal Git
    - $sudo pacman -S git
- Install Firefox
    - $sudo pacman -S firefox
- Install Java
    - $java -version                   #Show java version if it's installed
    - $sudo pacman -sS java | grep jre # List java jre versions available to download
    - $sudo pacman -S jre8-openjdk
    - $sudo pacman -sS java | grep jdk
    - $sudo pacman -S jdk8-openjdk
    - $java -version
- Install python    
    - $sudo pacman -S install python
- Install Mysql    
    - $sudo pacman -S mariadb
    - $mysql_install_db --user=mysql --basedir=/usr --datadir=/var/lib/mysql
    - $sudo  mysql_install_db --user=mysql --basedir=/usr --datadir=/var/lib/mysql
    - $systemctl enable --now mariadb
    - $mysql_secure_installation
    - $sudo mysql_secure_installation
    - $mysql
    - $mysql --version
- Sisale error "unable to lock database"
    - $sudo rm /var/lib/pacman/db.lck
- Install apache http server
    - $sudo pacman -S apache 
    - sudo systemctl enable httpd
    - sudo systemctl restart httpd
    - sudo systemctl status httpd
    - sudo nano /srv/http/index.html #Change default index page

